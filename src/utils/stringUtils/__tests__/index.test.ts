describe("String utils tests", () => {
  it("generateVerificationCode should generate 4 digit code", () => {});

  it("extractTokenFromAuthHeader should extract token correctly", () => {});

  it("parseTokenExpTimeToMs should parse token expiration time correctly", () => {});
});
